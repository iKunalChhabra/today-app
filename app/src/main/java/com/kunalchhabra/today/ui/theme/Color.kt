package com.kunalchhabra.today.ui.theme

import androidx.compose.ui.graphics.Color

// Adjusted Primary Colors
val PinkPrimary = Color(0xFFEB8ACD)  // Adjusted to match the logo
val PinkPrimaryLight = Color(0xFFF6B4E1)  // Slightly lighter shade for better contrast
val PinkPrimaryCard = Color(0xFFFFC8ED)  // Slightly lighter shade for better contrast
val PinkPrimaryDark = Color(0xFFD469AA)  // Slightly darker shade for better contrast

// Adjusted Secondary Colors
val PinkSecondary = Color(0xFFDA82BE)  // Consistent with the primary but slightly different hue
val PinkSecondaryLight = Color(0xFFE8A0CD)  // Slightly lighter shade
val PinkSecondaryDark = Color(0xFFC25E9E)  // Slightly darker shade

// Background Colors
val BackgroundDark = Color(0xFF303030)
val BackgroundLight = Color(0xFFFFF3E0)

// Surface Colors
val SurfaceDark = Color(0xFF424242)
val SurfaceLight = Color(0xFFFFF8E1)

// On Primary Colors
val OnPrimary = Color.White
val OnSecondary = Color.White

// On Tertiary Colors
val OnTertiaryDark = Color.White
val OnTertiaryLight = Color.Black

// On Background Colors
val OnBackgroundDark = Color.White
val OnBackgroundLight = Color.Black

// On Surface Colors
val OnSurfaceDark = Color.White
val OnSurfaceLight = Color.Black
